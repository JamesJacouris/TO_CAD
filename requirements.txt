# Core pipeline dependencies
numpy
scipy
numba
matplotlib
scikit-learn
scikit-image
networkx
open3d
trimesh           # External mesh input (STL/OBJ/PLY) via --mesh_input
pyvista>=0.43     # 3D FEA visualization (off-screen rendering)
pyevtk            # VTK file export for ParaView (optional)

# Parameter tuning (optional — only needed for tune_parameters.py)
optuna

# Documentation (only needed to build the Sphinx site)
sphinx
sphinx-rtd-theme
graphviz
